[tool.poetry]
name = "backend"
version = "0.1.0"
description = "Minimal Flask REST API"
authors = []
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
flask = "^3.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
